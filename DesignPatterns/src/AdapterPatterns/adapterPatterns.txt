适配器模式概念：
    把一个类的接口转换成客户端所期待的另一种接口，从而使原接口不匹配而无法在一起工作的两个类可以在一起工作。
适配器模式主要的几个要素：
    目标(CTarget)：定义一个客户端使用的特定接口。客户(Client)：使用目标接口，与和目标接口一致的对象合作。被适配者(CAdaptee)：一个现存需要匹配的接口。
    适配器(CAdapter)：负责将CAdaptee的接口转换成CTarget的接口。适配器是一个具体的类，是本模式的核心。
